#include <stdio.h>
#include <stdlib.h>

int muk(int &sayac){
    for i,toplam=0;
    for(i=1;gir)
}

int main()
{
    printf("gir: ");
    scanf("%d ",&gir);
    for (sayac=1;sayac<gir;sayac++)
    return 0;
}
